# Copy this file to ".env" ON THE SERVER (and locally for dev).
# NEVER commit the real .env -- it is git-ignored on purpose.
#
# Generate a fresh secret key with:
#   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

DJANGO_SECRET_KEY=replace-with-a-real-random-50-char-secret
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=134.209.66.211,localhost,127.0.0.1
