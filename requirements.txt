# Core web app dependencies (installed on the server during deploy).
Django>=6.0,<6.1
gunicorn>=23.0.0
whitenoise>=6.9.0
# MySQL driver (pure-Python, no compiler needed) + .env loading.
PyMySQL>=1.1.0
python-dotenv>=1.0.0
# REST API framework + OpenAPI/Swagger docs.
djangorestframework>=3.15,<4.0
drf-spectacular>=0.27,<1.0
